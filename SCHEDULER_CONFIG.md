# 📋 Рекомендации по настройке интервала проверки дайджестов

## Быстрый старт

Если у вас есть файл `.env`, вы можете разкомментировать или добавить эту строку:

```bash
# Интервал проверки дайджестов (в секундах)
# Значение по умолчанию: 1800 (30 минут)
DIGEST_CHECK_INTERVAL_SECONDS=1800
```

---

## 🎯 Рекомендованные значения

### Для production (рекомендуется)
```bash
DIGEST_CHECK_INTERVAL_SECONDS=1800   # 30 минут
```

**Оптимально для:**
- Снижения нагрузки на сервер
- Экономии трафика
- Долгосрочного использования

---

### Для быстрого тестирования
```bash
DIGEST_CHECK_INTERVAL_SECONDS=300    # 5 минут
```

**Используйте при:**
- Разработке
- Тестировании новых функций
- Проверке логики дайджестов

---

### Для максимальной экономии ресурсов
```bash
DIGEST_CHECK_INTERVAL_SECONDS=3600   # 60 минут
```

**適用 при:**
- Ограниченных ресурсах сервера
- Десятках тысяч пользователей
- Условиях VPS/shared hosting

---

### Для более частых проверок
```bash
DIGEST_CHECK_INTERVAL_SECONDS=900    # 15 минут
```

**Используйте при:**
- Важности оперативности дайджестов
- Малом количестве пользователей
- Желании балансировать между ресурсами и скоростью

---

## 📊 Сравнение вариантов

| Интервал | Проверок/день | Трафик/день | CPU/день | Макс. задержка | Рекомендация |
|----------|--------------|-----------|---------|--|---|
| **5 мин** | 288 | 0.6-1.5 MB | 20% | 5 мин | 🧪 Тестирование |
| **10 мин** | 144 | 0.3-0.7 MB | 10% | 10 мин | 💻 Разработка |
| **15 мин** | 96 | 0.2-0.5 MB | 7% | 15 мин | ⚖️ Баланс |
| **30 мин** | 48 | 0.1-0.24 MB | 3% | 30 мин | ✅ **Рекомендуется** |
| **60 мин** | 24 | 0.05-0.12 MB | 2% | 60 мин | 🔋 Эконом |

---

## 🔧 Как применить изменения

### Вариант 1: Изменить в .env
```bash
# 1. Откройте .env файл
nano .env

# 2. Найдите или добавьте строку:
DIGEST_CHECK_INTERVAL_SECONDS=1800

# 3. Сохраните (Ctrl+O, Enter, Ctrl+X)

# 4. Перезагрузите бота
python main.py
```

### Вариант 2: Установить в переменной окружения (Heroku)
```bash
# В Heroku Dashboard:
Settings → Config Vars → Add
KEY: DIGEST_CHECK_INTERVAL_SECONDS
VALUE: 1800
```

### Вариант 3: Установить через CLI (Docker)
```bash
docker run -e DIGEST_CHECK_INTERVAL_SECONDS=1800 your-bot
```

---

## 🔍 Как проверить текущее значение

### Посмотреть в логах при старте
```bash
python main.py 2>&1 | grep "Дайджесты"
# Вывод: Дайджесты: проверка каждые 30 мин (оптимизировано с 1 мин)
```

### Вычислить из исходного кода
```python
# Если DIGEST_CHECK_INTERVAL_SECONDS=1800, то:
1800 / 60 = 30 минут
```

---

## ⚙️ Если нужно вернуть старое поведение

**Старое значение (1 проверка в минуту):**
```bash
DIGEST_CHECK_INTERVAL_SECONDS=60
```

⚠️ **Не рекомендуется!** Это значительно увеличит нагрузку.

---

## 📝 Примеры .env файлов

### Вариант 1: Минимальный (оптимальный)
```bash
BOT_TOKEN=123456789:ABCdefGHIjklmnoPQRstuvWXYZ
APP_TIMEZONE=Europe/Moscow
DIGEST_CHECK_INTERVAL_SECONDS=1800
```

### Вариант 2: Со всеми параметрами
```bash
BOT_TOKEN=123456789:ABCdefGHIjklmnoPQRstuvWXYZ
APP_TIMEZONE=Europe/Moscow
DB_PATH=./bot_data.db
MESSAGE_RETENTION_DAYS=30
SUBSCRIPTION_FAILURE_RETENTION_DAYS=90
DIGEST_CHECK_INTERVAL_SECONDS=1800
```

### Вариант 3: Для тестирования
```bash
BOT_TOKEN=test_token_here
APP_TIMEZONE=Europe/Moscow
DB_PATH=:memory:
MESSAGE_RETENTION_DAYS=1
SUBSCRIPTION_FAILURE_RETENTION_DAYS=7
DIGEST_CHECK_INTERVAL_SECONDS=300   # 5 минут для быстрого тестирования
```

---

## 🔄 Поведение с разными интервалами

### Сценарий: Пользователь имеет подписку с `next_send_at = 10:00`

#### С интервалом 5 минут
```
09:55 → Проверка (no action)
10:00 → Проверка (отправить!) ✅
```

#### С интервалом 30 минут (рекомендуется)
```
09:30 → Проверка (no action)
10:00 → Проверка (отправить!) ✅
```

#### С интервалом 60 минут
```
09:00 → Проверка (no action)
10:00 → Проверка (отправить!) ✅ или 11:00 ⏸
```

**Вывод:** Даже с 30-минутным интервалом дайджесты отправляются в нужное время. ✅

---

## 🆘 Если что-то не работает

### Проблема: Дайджесты отправляются с задержкой

**Решение:**
```bash
# Уменьшить интервал
DIGEST_CHECK_INTERVAL_SECONDS=900   # 15 минут вместо 30
```

---

### Проблема: Слишком много проверок в логах

**Решение:**
```bash
# Увеличить интервал
DIGEST_CHECK_INTERVAL_SECONDS=3600  # 60 минут вместо 30
```

---

### Проблема: Не применяется новое значение

**Решение:**
1. Убедитесь, что `.env` сохранен
2. Перезагрузите бота полностью (не просто рестарт)
3. Проверьте, что переменная прочитана:
   ```python
   import os
   print(os.getenv("DIGEST_CHECK_INTERVAL_SECONDS", "not set"))
   ```

---

## 📊 Мониторинг с Prometheus (опционально)

Если у вас есть мониторинг, можно добавить метрику:

```python
from prometheus_client import Counter

digest_checks = Counter('digest_checks_total', 'Number of digest checks')

# В check_digests():
digest_checks.inc()
```

---

## 👍 Итоговые рекомендации

| Для | Значение | Интервал |
|-----|---------|----------|
| **Всем** ⭐ | `1800` | 30 минут |
| **Разработки** 🧪 | `300` | 5 минут |
| **Shared hosting** 🔋 | `3600` | 60 минут |
| **VIP сервер** 🚀 | `900` | 15 минут |

**Используйте 30 минут - это лучший баланс! ✅**

—

Все готово! Ваш бот теперь использует оптимизированный планировщик. 🎉
